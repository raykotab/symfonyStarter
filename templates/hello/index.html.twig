{% extends 'base.html.twig' %}

{% block body %}
<div>
The index twig says: </br>
    {% if messages|length > 0 %}
        {% for message in messages|slice(0, limit) %}
            {{ include('hello/_message.html.twig', {message: message})}}    
        {% endfor %}
    {% else %}
        nothing to see 
    {% endif %}
</div>
{% endblock %}
