{% extends 'base.html.twig'%}

{% block body %}
<div>The current twig message is:</div>
<div>
    {{ message.message }}
    <b>{{ include('hello/_message.html.twig', {message: message})}}</b>
</div>
{% endblock %}